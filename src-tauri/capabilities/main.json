{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "The main inspector window has no broad core/plugin grants; future capture commands must be granted explicitly.",
  "windows": ["main"],
  "permissions": []
}
